%h1 Listing people

%table
  %tr
    %th Company
    %th First name
    %th Last name
    %th Title
    %th Provider
    %th Available
    %th Available on
    %th Ok to contact
    %th Active
    %th
    %th
    %th

  - @people.each do |person|
    %tr
      %td= person.company_id
      %td= person.first_name
      %td= person.last_name
      %td= person.title
      %td= person.provider_id
      %td= person.available
      %td= person.available_on
      %td= person.OK_to_contact
      %td= person.active
      %td= link_to 'Show', person
      %td= link_to 'Edit', edit_person_path(person)
      %td= link_to 'Destroy', person, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Person', new_person_path
